Broaden platform JSON field names for Mutation Score Gate parser

Expand cosmic-ray/0/cosmic_ray.json with a wide set of PascalCase/
camelCase/snake_case field-name aliases per classification, matching
every term in the Excel "Raw Measurement Formula" column (Sensitivity
Score, Rigor Score, Weak Spot Count, Boundary Kill Rate, Resilience
Score, Semantic Pass Rate, Mutation Kill Rate), plus a `classifications`
lookup object keyed by the exact dashboard label. The dashboard's last
export showed identical 1/100 FAIL / 99/100 PASS scores before and
after the previous fix, suggesting the parser reads specific raw-data
keys rather than the aggregate "metrics" array - this maximizes the
chance of matching whichever key it actually looks up.
